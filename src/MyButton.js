
const MyButton = (props, link) => {
    return (
        <div>
            <a href={link} target="_blank"> <button>{props.name}</button> </a>
            
              
           
           
        </div>
    );
}

export default MyButton;





